package app.tryst

import org.junit.Assert.assertEquals
import org.junit.Test

/** Placeholder unit test proving the JVM test harness is wired. Replace as logic lands. */
class ExampleUnitTest {
    @Test
    fun arithmetic_sanity() {
        assertEquals(4, 2 + 2)
    }
}
