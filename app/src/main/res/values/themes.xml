<resources>
    <!-- Pre-Compose window theme. Compose (Material 3) drives the actual UI colors;
         edge-to-edge handles the system bars in MainActivity. -->
    <style name="Theme.Tryst" parent="android:Theme.DeviceDefault.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
