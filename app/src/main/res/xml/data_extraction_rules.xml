<?xml version="1.0" encoding="utf-8"?>
<!--
  Backup is already disabled via android:allowBackup="false". These rules are
  belt-and-suspenders: explicitly exclude all app data from both cloud backup and
  device-to-device transfer. Nothing leaves the device except the in-app encrypted export.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="sharedpref" />
        <exclude domain="database" />
        <exclude domain="file" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="sharedpref" />
        <exclude domain="database" />
        <exclude domain="file" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
