# Tryst — Doc + Store-Asset Sweep (2026-08-31)

> Holistic pass across every doc + README + CHANGELOG + fastlane metadata against the code
> at `c641268` (main == origin, tree clean, tag `v0.5.2` == `0afba59`, schema **v15**,
> versionCode **9** / versionName **0.5.2**).
>
> Goal: sync + audit — flag every stale line, drift, and gap; apply the surgical fixes;
> re-ship the F-Droid store assets pulled 2026-08-19 with captures from the current build.

## Baseline (verified this pass)

| Signal | Value |
|--------|-------|
| Git state | `main == origin == c641268`, tree clean |
| Last release tag | `v0.5.2` = `0afba59` (2026-07-30) |
| Commits since v0.5.2 | 15 — all chores / docs / deletions; no code shipped |
| Schema | **v15** (last bump v14→v15 in v0.5.0) |
| build.gradle.kts | `versionCode = 9`, `versionName = "0.5.2"` |
| F-Droid published | vc9 / 0.5.2 (bot MR !44579 auto-bumped from vc5, merged 2026-08-02) |
| Fastlane changelogs | `1.txt` … `9.txt` present |
| Store assets on disk | **none** — `fastlane/metadata/android/en-US/images/` does not exist |
| GitHub | 4★, 0 forks, 94 clones / 34 unique (14d), 1 open comment thread — none awaiting reply |

## Findings — docs

Ranked by user-visible impact.

### F1 · RELEASE.md — version stamps stale by two minor releases
**File:** `docs/RELEASE.md` §Per-release steps

Line 25 (`versionCode` — "currently `6`") and line 26 (`versionName` — "currently `0.4.0`")
were last touched at the v0.4.0 cut. Actual values in `app/build.gradle.kts`: `9` / `0.5.2`.
Also the example commit line 48 references `v0.1.0` — that's a template example, not stale
in itself, but re-reading it in context of a v0.5.2 release drifts the mental model.

**Fix:** bump to `9` / `0.5.2`; leave the example commit unchanged (it is an illustrative
snippet, not a claim about current state).

### F2 · RELEASE.md — prerequisites still say the repo is private
**File:** `docs/RELEASE.md` §Prerequisites (lines 9-13)

> "**The source repository must be public.** … **Making it public is the final pre-submission
> step** … current state is **private** — confirmed via the GitHub API on 2026-06-13."

The repo has been PUBLIC since the F-Droid submission ran in July. `AutoUpdateMode: Version`
requires a public source repo, which we've had for months.

**Fix:** mark the prerequisite as satisfied; drop the "briefly flipped then reverted" trivia
into a historical footnote or just delete it. Keep the *rule* (must be public) for anyone
starting a fork.

### F3 · RELEASE.md — "MR runbook" reads as forward instructions but is done
**File:** `docs/RELEASE.md` §MR runbook (lines 107-133) + §What the first submission actually
required (lines 135-152)

Written as a to-do list ("The submission cannot be automated…", "do it once, by hand"). MR
!40471 merged 2026-08-01, and the bot MR !44579 auto-bumped vc5→vc9 on 2026-08-02 exactly as
the recipe (`AutoUpdateMode: Version` + `UpdateCheckMode: Tags`) was designed to do.

**Fix:** re-head the section as **"First submission — 2026-06 (historical)"** so future
readers know the recipe already lives; add a short **"Ongoing releases"** paragraph noting
that after tagging `vX.Y.Z` with a bumped versionCode, F-Droid's checkupdates-bot opens a
recipe MR within a day (MR !44579 was the first live proof) — no manual fdroiddata work
needed unless the recipe itself changes.

### F4 · RELEASE.md — screenshots claim is out of date
**File:** `docs/RELEASE.md` (lines 103-105)

> "Screenshots are optional and **not** included … Add them later under
> `fastlane/metadata/android/en-US/images/phoneScreenshots/` if desired."

Screenshots were added 2026-07-02b (six of them), trimmed to five 2026-08-07 per F-Droid
issue #3, then all pulled 2026-08-19. Store gallery currently empty because the design
refresh is pending.

**Fix:** rewrite as: "Screenshots + `icon.png` currently withheld pending the QOL-4 icon
refresh; when reshipped they live under `fastlane/metadata/android/en-US/images/…`. See
STORE-1 / STORE-2 in ROADMAP_FUTURE." (Then supersede in the same sweep by actually reshipping
them, per F8 below.)

### F5 · ARCHITECTURE.md — package layout doesn't list the v0.4/v0.5 additions
**File:** `docs/ARCHITECTURE.md` §Package layout (lines 25-50)

Missing:
- `data/filter/` — the shared `EncounterFilter` query layer (FILT-1, v0.4.0)
- `data/search/` — `EncounterSearch` + query normalization (SRCH-1, v0.4.0)
- `data/gallery/` — `GalleryPhotos.build`, `GalleryPhoto`, `GalleryGroup` (GAL-1, v0.5.0)
- `data/media/PhotoMetadata` — EXIF read helper (META-1, v0.5.0)
- `ui/search/` — SearchScreen, SearchViewModel, MoreFiltersSheet, GalleryFiltersSheet
- `ui/gallery/` — GalleryScreen, GalleryViewModel, PhotoViewer, PersonPhotoStrip, AvatarViewer

Also:
- Line 30 `core/prefs/` omits `GalleryPreferences` (`tryst_gallery`)
- Line 32 says CatalogAdoption "v10–v11" — actual v10–v12 (added occasions +
  ejaculation-locations in v12/FDP-5)
- Line 80 says migration coverage "v1→v11" — actual v1→v15 (v12/v13/v14/v15 added since)
- Repositories list (line 34) omits `Kink` / `Toy` / `Occasion` / `EjaculationLocation` /
  `PersonPhoto` repositories

**Fix:** update the package tree and the two migration-coverage sentences. Surgical — no
prose rewrite.

### F6 · EXPORT_FORMAT.md — tables list missing `person_photo`, doesn't note `recent_searches` is excluded
**File:** `docs/EXPORT_FORMAT.md` §File layout (lines 44-56)

Line 44-47 lists the tables the backup dumps. **Missing:** `person_photo` (added v15/GAL-6),
which IS in `BackupManager.TABLES`. **Missing note:** `recent_searches` (v13/SRCH-1) is
deliberately EXCLUDED from backups (D-42) — worth calling out so a reader can't assume
"every table" without checking.

Media-blobs section (lines 51-56) mentions the two blob-provenance streams as "media rows
+ partner avatars". It should also cover:
- `profile.photoMediaId` (self-avatar blob, no media row) — since v7
- `person_photo.mediaBlobId` (portrait album blobs, no media row) — since v15

**Fix:** append `person_photo` to the tables list; add a bullet noting
`recent_searches` is deliberately not included; extend the "media blobs" bullet to name
all four provenance sources (media rows, partner avatars, self-profile avatar, portrait
albums).

### F7 · REQUIREMENTS.md — v0.4/v0.5 features not in the FR list
**File:** `docs/REQUIREMENTS.md` §4 Functional requirements

Current FR-1..FR-22 stop at the v0.3.2/v7 baseline (partners, self profile, backup). Nothing
in the FR list mentions:
- Search + filters (v0.4.0)
- Insights time scope (v0.4.0)
- Photos gallery + person photo albums (v0.5.x)

The status line at the top of the doc *does* claim "shipped through v0.5.2", so the FR list
lies by omission. Not a real "missing feature", but the doc is meant to be the requirements
canon.

**Fix:** add FR-23 (Photos gallery), FR-24 (Search), FR-25 (Insights time scope) as short
requirement lines pointing to CHANGELOG/DECISIONS for detail. Keep to one line each —
don't grow this into a duplicate CHANGELOG.

### F8 · README — screenshot table replaced with "pending" note; store gallery empty
**File:** `README.md` (lines 19-20), `fastlane/metadata/android/en-US/images/` (missing)

README's `<sub>` note under the hero says screenshots + icon are "pending the QOL-4 /
STORE-1 / STORE-2 design refresh". Given the user's decision this session to reship
now with captures from the current build (not blocked on QOL-4), this note becomes stale
the moment F8's fix lands.

**Fix (paired with the store-asset ship):** re-add a small screenshot table under Status,
and reinstate `fastlane/metadata/android/en-US/images/{icon.png, phoneScreenshots/}` from
fresh captures on emulator. Keep the icon as the placeholder-flame composite until QOL-4
lands (users expect on-device launcher to match store icon).

### F9 · full_description.txt — omits the Photos tab (marquee v0.5.x feature)
**File:** `fastlane/metadata/android/en-US/full_description.txt`

Current bullets cover encounters, categories, partners, Search, Insights, Achievements,
theming. **Missing:** the Photos gallery + per-person portrait album — the headline of the
v0.5.x line, and the reason for the majority of the 2026-07 doc/audit work. F-Droid users
never see the in-app "What's new" until after install; the store listing is where this
lands.

**Fix:** insert one bullet on Photos (browsable gallery + partner/self portrait albums +
pinch-zoom viewer + filters/search over photos).

## No fix needed

Verified current at v0.5.2 / v15:
- `README.md` (F-Droid badge auto-tracks; Status paragraph rewrote at v0.5.2 release;
  Documentation table accurate)
- `CHANGELOG.md` (`[Unreleased]` empty because no code shipped since v0.5.2; 0.5.0/0.5.1/0.5.2
  sections thorough and dated correctly)
- `docs/DATA_MODEL.md` (schema v15 documented incl. `person_photo`, D-53 rule, all migrations)
- `docs/FLOWCHARTS.md` (v0.5.2 header, 7-source Insights combine, ~66 achievements — all match)
- `docs/DECISIONS.md` (D-52 / D-53 / D-54 all present and dated 2026-07-30)
- `docs/THREAT_MODEL.md` (v0.5.2/v15, R-PIN + R-LOCK current)
- `docs/SECURITY_DESIGN.md` (v0.5.2/v15, PIN change through `reprotect` documented)
- `docs/ROADMAP.md` (2026-07-30 update line, M0-M8 done through v0.5.2)
- `docs/ROADMAP_FUTURE.md` (STORE-1/STORE-2/QOL-4 marked PULLED-pending, LIC-1 done,
  near-term ordering updated 2026-08-19)
- `docs/AUDIT_PLAN.md` (historical playbook, unaffected)
- `docs/PRERELEASE_PROMPT_PACK.md` (historical, unaffected)
- `docs/INTIMACY_IMPORT.md` (one-off migration doc, unaffected)
- `docs/SETUP_WINDOWS.md` (build steps unchanged)
- Fastlane `title.txt` / `short_description.txt` / `changelogs/*.txt` (all present, dated,
  in sync with in-app `ReleaseNotes.kt` and root CHANGELOG.md — D-35's four-spot invariant)

## Inconsistencies vs. duplication

None flagged. The docs deliberately overlap (README → CHANGELOG → DECISIONS → ROADMAP each
carry different lenses); DOC-2 already trimmed and unified them 2026-07-02b, and no drift
has surfaced since.

## Gaps (deliberate non-fixes)

- **QOL-4 launcher-icon refresh** — still uses the M1 placeholder-flame. Not touched this
  sweep (user's design bundle is a separate future arc).
- **D-54 ejaculation-model expansion** — deferred to v0.6 by explicit decision; no doc
  action.
- **Store icon.png** — reshipping the placeholder-flame composite this sweep; a proper
  designer icon still belongs to STORE-2 / QOL-4.

## Store-asset ship plan (STORE-1 + STORE-2)

Deliberately shipping FROM THE CURRENT v0.5.2 BUILD, not restoring the pre-2026-08-19 set:
the app has grown a Photos tab + full-screen partner editor + person portrait albums since
the last screenshots were taken.

Capture sequence on `Pixel_9_Pro_XL` AVD, restored from the test-dataset backup (pw
`Tryst-Test-2026`), `FLAG_SECURE` temporarily disabled per the Pass-5 procedure:

1. Trysts (list view)
2. Trysts (calendar view)
3. Encounter editor (mid-scroll, showing the category chips)
4. Photos tab (date-grouped grid)
5. Photo viewer with the info panel open (META-1)
6. Insights (top of page, showing scope chips + a couple of stat tiles)
7. Partners (list + "You" card)
8. Settings (top screen)

Skip anything the F-Droid all-ages review flagged in issue #3 (no explicit-category screens
in isolation). Icon: 512×512 PNG composited from `ic_launcher_foreground.xml` (white flame)
on `#1A1A2E` background, matching the on-device launcher.

## Suggested commit plan

Two logical commits:
1. `docs: post-v0.5.2 sync + audit (RELEASE, ARCHITECTURE, EXPORT_FORMAT, REQUIREMENTS,
   README, full_description)`
2. `chore(store): reship fastlane screenshots + icon.png from current v0.5.2 build`

No release tag; no code changed.
